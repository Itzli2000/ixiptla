---
interface Props {
  href?: string;
  variant?: 'primary' | 'secondary' | 'ghost';
  size?: 'sm' | 'md' | 'lg';
  class?: string;
}

const { 
  href, 
  variant = 'primary', 
  size = 'md',
  class: className = '' 
} = Astro.props;

const baseStyles = 'inline-flex items-center justify-center font-medium transition-all duration-200 rounded-lg';

const variants = {
  primary: 'bg-main text-quaternary hover:bg-dark shadow-lg hover:shadow-xl',
  secondary: 'bg-secondary text-tertiary hover:bg-secondary/80',
  ghost: 'bg-transparent text-dark hover:bg-dark/10'
};

const sizes = {
  sm: 'px-4 py-2 text-sm',
  md: 'px-6 py-3 text-base',
  lg: 'px-8 py-4 text-lg'
};

const classes = `${baseStyles} ${variants[variant]} ${sizes[size]} ${className}`;

const Component = href ? 'a' : 'button';
---

<Component href={href} class={classes}>
  <slot />
</Component>